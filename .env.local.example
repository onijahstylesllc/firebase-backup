# Supabase Configuration
# These values are required for the application to function properly
# Copy this file to .env.local and fill in your actual values

# Supabase Project URL (used by both client and server)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (client-side only - respects RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (server-side only - bypasses RLS)
# WARNING: Never expose this key to the client
# Only use in API routes or server-side code that needs to bypass RLS
# For Supabase Edge Functions, this is automatically provided by Supabase
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Additional CDN or API endpoints
# Uncomment and configure if you're using additional external services
# NEXT_PUBLIC_CDN_URL=https://cdn.example.com
